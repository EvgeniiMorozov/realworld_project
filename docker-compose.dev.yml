version: "3"

services:
  db:
    image: postgres:13.5
    ports:
      - 5432:5432
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: Zz123456
      POSTGRES_DB: realworld_project